에러 구분 
문법(컴파일타입) 에러     /    실행(런타임) 에러


조건문
 if 문 
 - tru 조건식    :   boolean 타입  T/F 로 값 반환


 if 문  이 하나일 경우, 좌중괄호 우중괄호 생략가능


 &, && 공통점
 - 두 조건 모두 참일때만, 실행문이 실행됨.
  차이점
  - 한 개이면, 앞에 조건이 거짓이더라도 뒤에 조건을 따진다. 
  - 두 개이면, 앞에 조건이 거짓인 경우 판정이 가능하므로 
    굳이 뒤에 조건을 따지지 않는다. - 일량이 줄어든다. 
     T T - TRUE
     F T - FALSE
     T F - FALSE
     F F - FALSE


     ||  - 조건 둘중 한쪽만 참이면, 참


     반복문
     WHILE     - 사용시, 조건식에 사용된 변수의 증감연산자가 있는지 꼭 확인해볼것  (무한루프에빠질수있음)
     FOR 
     DoWhile    -  while은 먼저 / dowhile은 조건을 나중에 따진다.  딱 한번은 무조건 실행 


     >>생성자는 이름이 반드시 클래스명과 동일해야한다. 
     오버로딩 가능

     **12/6  <화>
     생성자 
     1. 메소드와 구분할 수 있는 방법 - 반환 타입이 없다. (int, double, 이런거 다 X)
     2. 같은 이름의 생성자를 중복해서 선언할 수 있다. 
        (단, 파라미터의 개수가 다르거나 타입이 달라야만 한다.)
     3. 전역변수의 초기화이다. 
     4. 디폴트 생성자(파라미터가 하나도 없는 생성자) 는 생략할 수 있다.  (jvm이 입력해줌)
     5. 생성자의 파라미터를 통해서 클래스 사이의 관계를 설정할 수 있다. 


참조형의 선언문은 ..........

클래스 선언은 (초기화를 하지 않은 선언은) 생성자를 호출하지 않는다. 

생성자도 파라미터를 가질 수 있따. ?